# Tools

Clone the upstream repositories into these folders:

- `DiffSinger/`
- `MakeDiffSinger/`
- `SOME/`

Keeping them under `tools/` makes the project structure predictable without mixing their histories into your dataset bookkeeping.
